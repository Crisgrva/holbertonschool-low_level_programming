#include <stdio.h>
#include "holberton.h"

/**
 * add - Adds two integers and returns the result.
 * @a: number to add
 * @b: number to add
 * Return: Always 0 (Success)
 */

int add(int a, int b)
{
	return (a + b);
}
